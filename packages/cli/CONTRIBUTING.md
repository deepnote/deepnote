# Contributing to @deepnote/cli

## Development

```bash
# Install dependencies (from repo root)
pnpm install

# Build the CLI
pnpm build

# Run the CLI in development mode (without building)
pnpm dev --help
pnpm dev --version
pnpm dev inspect path/to/file.deepnote

# Run tests
pnpm test
```
