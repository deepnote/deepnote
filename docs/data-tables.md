---
title: Data tables
description: View and explore tabular data
noIndex: false
noContent: false
---

Data tables are part of SQL and Code block outputs. They display DataFrame contents in a notebook.

## Preview

Some tables show a **Preview** label. In SQL blocks, this indicates Query preview mode. Query preview returns a limited result and lets you reuse the query in later SQL blocks. See [SQL blocks](/docs/sql-cells#output-modes) for details.

## Column menu

Click a column name to open its menu. The current menu includes:

- Sort ascending
- Sort descending
- Filter
- Rename
- Copy name to clipboard
- Hide column
- Wrap cells

## Current limits

In the tested DataFrame SQL output, the table-level formatting, copy, and export controls were disabled. The current UI did not expose the older cell-formatting, column-reordering, or cell-copying workflows described in this page, so those steps and their videos have been removed until they can be checked again.
